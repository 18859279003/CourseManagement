<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xmu.crms.mapper.TopicMapper">

    <select id="getTopicByTopicId" resultType="xmu.crms.entity.Topic">
        SELECT
        id,
        name,
        description,
        group_number_limit,
        group_student_limit
        FROM topic
        WHERE id= #{topicId};
    </select>
    
    <update id="updateTopicByTopicId"  parameterType="xmu.crms.entity.Topic">
       UPDATE  topic
       SET
        name = #{topic.name},
        description = #{topic.description},
        group_number_limit =#{topic.groupNumberLimit},
        group_student_limit =#{topic.groupStudentLimit}
 		WHERE id= #{topicId};
    </update>
    
    <delete id="deleteTopicByTopicId"  parameterType="xmu.crms.entity.Topic">
       DELETE FROM topic
 	   WHERE id= #{topicId};
    </delete>
    
    <select id="listTopicBySeminarId" resultType="xmu.crms.entity.Topic">
        SELECT
        id,
        name,
        description,
        group_number_limit,
        group_student_limit
        FROM topic
        WHERE seminar_id= #{seminarId};
    </select>
    
     <insert id="insertTopicBySeminarId"  parameterType="xmu.crms.entity.Topic">
       INSERT INTO topic(
        id,
        name,
        description,
        group_number_limit,
        group_student_limit,
        seminar_id
        )
 	  	VALUES(
 	  	#{topic.id},
 	  	#{topic.name},
 	  	#{topic.description},
 	  	#{topic.groupNumberLimit},
 	  	#{topic.groupStudentLimit},
 	  	#{seminarId}
 	  	)
 	   ;
    </insert>
    
     <delete id="deleteSeminarGroupTopicById">
       DELETE FROM seminar_group_topic
 	   WHERE topic_id= #{topicId}
 	   AND seminar_group_id=#{groupId};
    </delete>
    
     <delete id="deleteSeminarGroupTopicByTopicId">
       DELETE FROM seminar_group_topic
 	   WHERE topic_id= #{topicId};
    </delete>
    
    <select id="getSeminarGroupTopicById" resultType="xmu.crms.entity.SeminarGroupTopic">
	SELECT
	id,
	presentation_grade
	FROM seminar_group_topic
	WHERE topic_id= #{topicId}
    AND seminar_group_id=#{groupId};
	</select>    
   <select id="getSeminarGroupTopiclistByGroupId" resultType="xmu.crms.entity.SeminarGroupTopic">
	SELECT
	topic_id
	FROM seminar_group_topic
	WHERE seminar_group_id=#{groupId};
	</select>    
</mapper>

 